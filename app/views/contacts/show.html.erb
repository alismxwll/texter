<h1><%= @contact.name %></h1>

<ul>
  <li><%= @contact.name %></li>
  <li><%= @contact.phone_number %></li>
</ul>

<%= render 'messages/form' %>

<p><%= link_to "Delete Contact", root_path,
                           :data => {:confirm => "You sure?",
                                     :method => "delete"},
                           :class => "btn btn-danger" %></p>
